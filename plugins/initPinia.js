// plugins/initialize-store.ts
import { defineNuxtPlugin } from '#app';

export default defineNuxtPlugin(async (nuxtApp) => {
  // const postsStore = configDataStore();
  // await postsStore.fetchPosts();
});
